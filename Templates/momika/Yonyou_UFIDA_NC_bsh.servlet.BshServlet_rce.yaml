name: Yonyou_UFIDA_NC_bsh.servlet.BshServlet_rce
transport: http
set: {}
detail:
  dock: app="Yonyou-UFIDA-NC"
  product: ''
  homepage: https://github.com/adminwaf/goby_exp/
  impact: ''
  description: 用友 NC bsh.servlet.BshServlet 存在远程命令执行漏洞，通过BeanShell 执行远程命令获取服务器权限
rules:
  r1:
    method: GET
    path: /servlet/~ic/bsh.servlet.BshServlet
    follow_redirects: false
    headers: {}
    expression: response.status == 200 && response.body.bcontains(b'BeanShell')
expression: r1()
